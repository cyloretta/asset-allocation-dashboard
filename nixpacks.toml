[phases.setup]
nixPkgs = ["python39", "nodejs-18_x", "npm"]

[phases.install]
cmds = [
    "cd frontend && npm install && npm run build",
    "cd backend && pip install -r requirements.txt"
]

[start]
cmd = "cd backend && python main.py"
